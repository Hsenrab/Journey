import { Card, CardContent, Stack, Typography } from '@mui/material'

export default function MapPage() {
  return (
    <Stack spacing={3}>
      <Typography variant="h4">Map</Typography>
      <Card>
        <CardContent>
          <Typography>
            Map visualisation is not implemented yet. Activities still store mandatory location records for future map
            views.
          </Typography>
        </CardContent>
      </Card>
    </Stack>
  )
}
